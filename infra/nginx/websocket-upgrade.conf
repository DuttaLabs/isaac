# Belongs in the http context, hence its own file in conf.d.
#
# An upgrade request carries "Upgrade: websocket" and needs "Connection:
# upgrade" passed through; anything else must not, or an ordinary request is
# proxied as a failed handshake.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
